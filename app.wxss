/**app.wxss**/
/* 全局样式 - 古字通 */

/* 安全区域适配（顶部状态栏留白由各页面用 statusBarHeight 处理，自定义导航栏下 env 顶部不可靠） */
page {
  background-color: #fcf9f8;
  font-family: 'Songti SC', 'SimSun', 'Noto Serif SC', serif;
  font-size: 28rpx;
  color: #1b1c1c;
  line-height: 1.6;
  padding-bottom: env(safe-area-inset-bottom);
  padding-left: env(safe-area-inset-left);
  padding-right: env(safe-area-inset-right);
}

/* 全局容器 */
.container {
  min-height: 100vh;
  background-color: #fcf9f8;
  box-sizing: border-box;
}

/* Flex 布局工具类 */
.flex {
  display: flex;
}

.flex-center {
  display: flex;
  align-items: center;
  justify-content: center;
}

.flex-between {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.flex-column {
  display: flex;
  flex-direction: column;
}

/* 文字颜色 */
.text-primary {
  color: #2C3E50;
}

.text-accent {
  color: #B91C1C;
}

.text-secondary {
  color: #666;
}

.text-muted {
  color: #999;
}

/* 通用卡片样式 */
.card {
  background-color: #fff;
  border-radius: 16rpx;
  padding: 30rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.06);
}

/* 通用按钮样式 */
.btn-primary {
  background-color: #B91C1C;
  color: #fff;
  border-radius: 12rpx;
  padding: 24rpx 0;
  text-align: center;
  font-weight: bold;
  letter-spacing: 4rpx;
}

.btn-primary:active {
  background-color: #991b1b;
  transform: scale(0.98);
}

/* 底线样式 */
.divider {
  height: 1rpx;
  background-color: #f0f0f0;
  margin: 20rpx 0;
}

/* 滚动区域 */
.scroll-view {
  flex: 1;
  overflow-y: auto;
}
