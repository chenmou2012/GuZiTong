<!-- pages/collections/collections.wxml -->
<view class="container" style="padding-top: calc(20rpx + {{statusBarHeight}}px)">
  <!-- 返回按钮 -->
  <view class="back-btn" bindtap="goBack">
    <text class="back-text">‹ 返回</text>
  </view>

  <!-- 页面标题 -->
  <view class="page-header">
    <text class="page-title">我的收藏</text>
  </view>

  <records-list
    list="{{collections}}"
    title-field="word"
    subtitle-field="result"
    empty-text="还没有收藏，常用字可点击心形收藏"
    bind:itemtap="onItemTap"
    bind:itemdelete="onItemDelete"
    bind:clearall="onClearAll"
  />
</view>